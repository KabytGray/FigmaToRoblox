# segredos — se isso vazar, a chave do Roblox vai para o mundo
apikey.txt
config.json

# gerado pelo INSTALAR.bat a partir de wrangler.example.toml — cada usuário tem
# o próprio id de KV, não faz sentido versionar
cloudflare-worker/wrangler.toml

# geral
node_modules/
dist/
out/
.env
*.rbxl
.DS_Store

# artefatos de teste e outputs
body.bin
headers.txt
request.json
test.png
teste.png
package-lock.json
.wrangler/

# notas internas — ficam so no seu PC
HANDOFF.md
COMPETITORS.md

# icones para publicar — ficam so no seu PC
icones/

# ferramentas so suas: diagnostico e geracao de assets. Nao servem a quem
# instala e revelam o processo por tras do projeto.
diagnose.js
figma-plugin/tools/

# arquitetura antiga, substituida pelo arquivo unico + templates. Nao entra no
# build; publicar so daria a estranhos um mapa de como o projeto foi montado.
roblox-plugin/src/
roblox-plugin/default.project.json
